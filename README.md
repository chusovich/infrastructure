# Infrastructure
All of my code to setup my homelab infrastructure

## Overview
- Every server runs NixOS
- All applications are deployed using docker

## Repo Structure
- Each server has its own folder (atlas, etc.)
- The "containers" and "nix" folders container reused docker compose files and nix modules
